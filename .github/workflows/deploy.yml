name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Frontend Image
        run: |
          docker build -t shubham0039sharma/todos-frontend:latest ./todo-frontend

      - name: Build Backend Image
        run: |
          docker build -t shubham0039sharma/todos-backend:latest ./todo-backend

      - name: Push Images to DockerHub
        run: |
          docker push shubham0039sharma/todos-frontend:latest
          docker push shubham0039sharma/todos-backend:latest
